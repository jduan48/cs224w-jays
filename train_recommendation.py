def

def train(profiles, convos):
    global_vector, user_vector = [], dict()
    for conv in convos
        for user, line in [(convo["profile1"], convo["lines1"]), (convo["profile2"], convo["line2"])]:

    global_avg =  sum([convo["lines1"] if convo["lines1"] else 0 + \
                convo["lines2"] if convo["lines2"] else 0 for convo in convos]) \
                / float(len(convos))

